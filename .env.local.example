# Copy to .env.local and fill in from the Supabase dashboard:
# Project Settings -> API

NEXT_PUBLIC_SUPABASE_URL=https://nyvhnumdltwshrmozhgw.supabase.co

# Server-only. Step 3 has no auth, so the app reads and writes as the service
# role. This key bypasses row-level security - never expose it to the browser
# and never commit .env.local.
SUPABASE_SERVICE_ROLE_KEY=
